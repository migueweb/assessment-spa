{
  "name": "assessment",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "migrate": "cp ./db/db.example.json ./db/db.json",
    "api": "json-server ./db/db.json"
  },
  "devDependencies": {
    "json-server": "1.0.0-beta.3",
    "vite": "7.0.4"
  },
  "dependencies": {
    "axios": "1.10.0"
  }
}
